# Secrets
**/.env*

# Protected assets
/assets/protected/**/*

# Self-signed certs that can't be shared
/ssl/*.pem

# Editor droppings
.idea

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Builds and dependencies
dist
**/dist/*
**/build/*
**/.cache/*
**/node_modules

# Development database. Only the initial version is checked into source, all
# local changes after that are ignored.
db/database.db

# Maxmind database files
projects/nginx/mmdb/**/*

# Deno dependency/build cache
# TODO look into vendoring instead, for good measure.
projects/server_deno/.deno-cache
